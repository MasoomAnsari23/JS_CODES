const mainHeading=document.querySelector("#main-header");
console.log(mainHeading);

const header=document.querySelector(".header");
console.log(header);

const navIteam=document.querySelector(".nav-item");
console.log(navIteam);  //this will only read the first class value with nav-item

const navitem=document.querySelectorAll(".nav-item");
console.log(navitem);  //this will read every class with names as nav-item